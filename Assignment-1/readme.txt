Backpropagation.py:

Design a four layers network To learn how to use the BP algorithm(from scratch) and train the network with one classification dataset(Here- IRIS) selected from the UCI dataset library.

ELM-IRIS.py:

Design a single-layer network trained by a fixed ELM method.
